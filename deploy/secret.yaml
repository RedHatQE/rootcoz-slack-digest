# Create from literals / sealed-secrets — do not commit real values.
#   oc create secret generic rootcoz-slack-digest-credentials \
#     --from-literal=ROOTCOZ_URL=https://rootcoz.example.com \
#     --from-literal=ROOTCOZ_USERNAME=... \
#     --from-literal=ROOTCOZ_API_KEY=... \
#     --from-literal=SLACK_BOT_TOKEN=xoxb-... \
#     --from-literal=SLACK_CHANNEL=C01234567 \
#     --from-literal=JENKINS_URL=https://... \
#     -n REPLACE_NAMESPACE --dry-run=client -o yaml
apiVersion: v1
kind: Secret
metadata:
  name: rootcoz-slack-digest-credentials
  namespace: REPLACE_NAMESPACE
  labels:
    app: rootcoz-slack-digest
type: Opaque
stringData:
  ROOTCOZ_URL: "REPLACE"
  ROOTCOZ_USERNAME: "REPLACE"
  ROOTCOZ_API_KEY: "REPLACE"
  ROOTCOZ_VERIFY_SSL: "false"
  SLACK_BOT_TOKEN: "REPLACE"
  SLACK_CHANNEL: "REPLACE"
  JENKINS_URL: "REPLACE"
  # Fine-grained or classic PAT with contents:read on this private repo
  GITHUB_TOKEN: "REPLACE"
