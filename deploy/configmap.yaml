apiVersion: v1
kind: ConfigMap
metadata:
  name: rootcoz-slack-digest-config
  namespace: cnv-rootcoz
  labels:
    app: rootcoz-slack-digest
data:
  config.toml: |
    [digest]
    timezone = "UTC"
    max_rows = 25
    sort_by = "not_reviewed"
    tiers = ["gating", "release-checklist", "other"]
    teams = []
    rootcause_summary_url = "https://example.html"

    [slack]
    mode = "bot"

    [mentions.teams]
    # virt-node = "virt-node-qe"
    # network = "network-qe"
